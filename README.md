# web-browser
Simplified web browser which I'm working on
Current Features: 
* Connection establising between client and server over IP/TCP
* Client requests and responses which supports schemes such as data, file, http, https, view-source. 
* Handles simple HTML parsing and HTML entities, response caching, redirects.
* Simple GUI wich supports scrolling and resizing
